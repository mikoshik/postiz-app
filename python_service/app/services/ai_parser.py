"""
AI парсер для извлечения данных из текста объявлений.
"""
import json
import re
from typing import Dict, Any
from langchain_openai import ChatOpenAI
from langchain.agents import create_agent
from langchain_core.messages import HumanMessage, SystemMessage
from .ai_tools import search_car_generations
from app.config.settings import OPENAI_API_KEY


# Промпт для парсинга с готовой схемой полей
SCHEMA_BASED_PROMPT = """
Ты — интеллектуальный парсер объявлений о продаже автомобилей.

ТВОЯ ЗАДАЧА:
Проанализировать текст объявления и заполнить значения в предоставленной схеме полей.

ВХОДНЫЕ ДАННЫЕ:
1. Текст объявления от пользователя
2. Схема полей в формате JSON, где:
   - Ключ = название поля (например: "make", "model", "year")
   - Значение = объект с "value" (пустая строка для заполнения) и "options" (список допустимых значений)
   - ИЛИ просто пустая строка "" для текстовых/числовых полей без ограничений

ПРАВИЛА ЗАПОЛНЕНИЯ:

1. **Поля с options (выпадающий список)**:
   - Выбирай ТОЛЬКО из предоставленного списка options
   - Возвращай ТОЧНОЕ название из списка (с учётом регистра)
   - Если в тексте указано значение, которого нет в списке — оставь пустую строку ""
   - Пример: если options = ["BMW", "Audi", "Mercedes"], а в тексте "бмв" → верни "BMW"

2. **Текстовые/числовые поля (без options)**:
   - Извлекай значение напрямую из текста
   - Для чисел — только цифры (например: "2020", "50000", "25000")
   - Для текста — краткое значение

3. **Если информация отсутствует**:
   - Возвращай пустую строку ""
   - НЕ придумывай данные, которых нет в тексте

4. **Особые случаи**:
   - Марка/модель: ищи названия брендов и моделей авто
   - Год: 4-значное число (1990-2025)
   - Цена: число без валюты
   - Пробег: число в километрах
   - Объём двигателя: ищи формат "X.X л" или "XXXXcc"
   - КПП: "автомат"/"механика"/"вариатор"/"робот"
   - Тип топлива: бензин/дизель/газ/электро/гибрид

ФОРМАТ ОТВЕТА:
Верни ТОЛЬКО валидный JSON объект с теми же ключами, что и во входной схеме.
Значения — это распознанные данные или пустая строка.

Пример входной схемы:
{
    "make": {"value": "", "options": ["BMW", "Audi", "Mercedes"]},
    "model": "",
    "year": "",
    "price": "",
    "mileage": "",
    "transmission": {"value": "", "options": ["Автомат", "Механика", "Вариатор"]}
}

Пример текста: "Продаю БМВ X5 2020 года, автомат, пробег 45000 км, цена 25000 евро"

Пример ответа:
{
    "make": "BMW",
    "model": "X5",
    "year": "2020",
    "price": "25000",
    "mileage": "45000",
    "transmission": "Автомат"
}
"""


class AIParserService:
    """Сервис для AI парсинга текста объявлений."""

    def __init__(self):
        self.agent = create_agent(
            model=ChatOpenAI(
                model="gpt-4o",
                temperature=0,
                api_key=OPENAI_API_KEY
            ),
            tools=[search_car_generations],
            system_prompt=SystemMessage(content=SCHEMA_BASED_PROMPT),
        )

    def parse_with_schema(self, text: str, schema: dict) -> dict:
        """Парсит текст используя предоставленную схему полей."""
        try:
            user_message = f"""
    ТЕКСТ ОБЪЯВЛЕНИЯ:
    {text}

    СХЕМА ПОЛЕЙ ДЛЯ ЗАПОЛНЕНИЯ:
    {json.dumps(schema, ensure_ascii=False, indent=2)}

    Заполни схему на основе текста. Верни только JSON.
    """
            
            # Pass a list of messages - not a single message
            response = self.agent.invoke({
                "messages": [{"role": "user", "content": user_message}]
            })
            
            # Extract content from response dict
            output = response["messages"][-1].content  # Get last message content
            
            # Clean JSON
            result_text = self._clean_json_response(output)
            result = json.loads(result_text)
            
            print(f"🤖 AI Schema Parse Result: {result}")
            return result
            
        except Exception as e:
            print(f"❌ AI Schema Parse Error: {str(e)}")
            import traceback
            traceback.print_exc()
            return {}
    def _clean_json_response(self, text: str) -> str:
        """Очищает ответ от markdown и лишних символов."""
        if "```json" in text:
            text = text.split("```json")[1].split("```")[0].strip()
        elif "```" in text:
            text = text.split("```")[1].split("```")[0].strip()
        return text

# Singleton instance
ai_parser_service = AIParserService()
